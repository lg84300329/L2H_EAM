<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.l2h.eam.sys.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="User" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Aug 26 20:55:40 CST 2015.
        -->
        <id column="USER_ID" property="user_id" jdbcType="INTEGER" />
        <result column="USER_NAME" property="user_name" jdbcType="VARCHAR" />
        <result column="PASSWORD" property="password" jdbcType="VARCHAR" />
        <result column="ACCOUNT" property="account" jdbcType="VARCHAR" />
        <result column="POST_CODE" property="post_code" jdbcType="VARCHAR" />
        
    </resultMap>
    <!---->
    <resultMap id="usersResultMap" type="User">
        <collection property="users" ofType="User" javaType="java.util.List"/>


    </resultMap>
    <!--根据7-->
    <insert id="insertUser" parameterType="User" statementType="PREPARED" >

         INSERT INTO
          t_sys_user( USER_NAME,PASSWORD,ACCOUNT,POST_CODE)
         VALUES (#{USER_NAME},#{PASSWORD},#{ACCOUNT},#{POST_CODE})
    </insert>

    <update id="updateUser" parameterType="User" statementType="PREPARED">
        UPDATE t_sys_user
        <set>
            <if test="user_name!=null and user_name!=''">user_name=#{user_name},</if>
            <if test="password!=null  and password !=''">password=#{password},</if>
            <if test="account!=null   and account!=''">account=#{account}</if>
            <if test="post_code!=null and post_code!=''">post_code=#{post_code},</if>

        </set>
        WHERE user_id=#{user_id}
    </update>

    <select id="selectUserByAccount" resultType="User" parameterType="java.lang.String">
        SELECT
           USER_ID,
           USER_NAME,
           PASSWORD,
           ACCOUNT,
           POST_CODE
         FROM  t_sys_user WHERE  account = #{account}
    </select>

    <select id="selectUserById" resultType="User" parameterType="int">
        SELECT
        USER_ID,
        USER_NAME,
        PASSWORD,
        ACCOUNT,
        POST_CODE
        FROM  t_sys_user WHERE  USER_ID = #{USER_ID}
    </select>

    <select id="getUsersByPost" resultMap="usersResultMap" parameterType="String">

        SELECT *
        FROM t_sys_user
        <where>
           <if test="post_code!=null and post_code!='' " >post_code=#{post_code}</if>
        </where>

    </select>
</mapper>