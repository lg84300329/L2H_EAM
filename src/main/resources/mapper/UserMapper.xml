<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.l2h.eam.sys.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.l2h.eam.sys.domain.User" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Aug 26 20:55:40 CST 2015.
        -->
        <id column="USER_ID" property="user_id" jdbcType="INTEGER" />
        <result column="USER_NAME" property="user_name" jdbcType="VARCHAR" />
        <result column="PASSWORD" property="password" jdbcType="VARCHAR" />
        <result column="ACCOUNT" property="account" jdbcType="VARCHAR" />
        <result column="POST_CODE" property="post_code" jdbcType="VARCHAR" />
        
    </resultMap>
    <!---->

    <!--根据7-->
    <insert id="insertUser" parameterType="com.l2h.eam.sys.domain.User" statementType="PREPARED" >

         INSERT INTO
          t_sys_user( USER_NAME,PASSWORD,ACCOUNT,POST_CODE)
         VALUES (#{USER_NAME},#{PASSWORD},#{ACCOUNT},#{POST_CODE})
    </insert>

    <update id="updateUser" parameterType="com.l2h.eam.sys.domain.User" statementType="PREPARED">
        UPDATE t_sys_user
        <set>
            <if test="user_name!=null and user_name!=''">user_name=#{user_name},</if>
            <if test="password!=null  and password !=''">password=#{password},</if>
            <if test="account!=null   and account!=''">account=#{account}</if>
            <if test="post_code!=null and post_code!=''">post_code=#{post_code},</if>

        </set>
        WHERE user_id=#{user_id}
    </update>

    <select id="selectUserByAccount" resultType="com.l2h.eam.sys.domain.User" parameterType="java.lang.String">
        SELECT
           USER_ID,
           USER_NAME,
           PASSWORD,
           ACCOUNT,
           POST_CODE
         FROM  t_sys_user WHERE  account = #{account}
    </select>

    <select id="selectUserById" resultType="com.l2h.eam.sys.domain.User" parameterType="int">
        SELECT
        USER_ID,
        USER_NAME,
        PASSWORD,
        ACCOUNT,
        POST_CODE
        FROM  t_sys_user WHERE  USER_ID = #{id}
    </select>

    <select id="getUsersByPost" resultMap="BaseResultMap" parameterType="java.lang.String">

        SELECT *
        FROM t_sys_user
        WHERE post_code=#{post_code}


    </select>
    <select id="getUsersByDpet" resultMap="BaseResultMap" parameterType="java.lang.String">
     SELECT c.POST_NAME,d.ACCOUNT,d.PASSWORD,d.USER_ID,d.USER_NAME
     FROM t_sys_dpet a ,t_sys_dpet_post b,t_sys_post c,t_sys_user d
     WHERE  a.DPET_CODE=#{dept_code} AND a.DPET_CODE=b.DPET_CODE and b.POST_CODE = c.POST_CODE and c.POST_CODE=d.POST_CODE
    </select>

    <resultMap id="dpetMap" type="com.l2h.eam.utils.domain.TreeNode">
        <id property="node_id" column="DPET_ID"/>
        <result property="parentID" column="PARENT_DPET_ID"/>
        <result property="node_name" column="DPET_NAME"/>
    </resultMap>


</mapper>